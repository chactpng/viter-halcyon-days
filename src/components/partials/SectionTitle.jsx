import React from 'react'

const SectionTitle = ({ title, image }) => {
    return (
      <>
      <h3 className="text-[15px] mb-3 uppercase font-semibold">
         {title}
        </h3>
      </>
    );
  };
export default SectionTitle
